# Translations

CI-generated compiled rule output, mirroring the `detections/` path (a rule at `detections/<tactic>/<source>/<name>/` compiles to `translations/<tactic>/<source>/<name>.<lang>`). Committed by deploy.py on merge to main as show-your-work evidence of multi-SIEM portability. Never hand-edited.
